# Installation

Install `nuxt-files-sdk` with Bun 1.4 and add it to `modules` in `nuxt.config.ts`.
Runtime storage belongs in the project-root `files.config.ts`, not in Nuxt module options.

The integration delegates adapters, plugins, errors, file models, and operations to Files SDK. It never changes connection mode or falls back to another backend.
