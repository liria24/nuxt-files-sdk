# Storage and development overrides

Every entry in `storage` is named. `await useServerFiles("blob")` resolves and memoizes its native Files SDK instance. A sole storage, a storage named `default`, or the explicit `default` option may be resolved without a name.

In development only, `devStorage[name] ?? storage[name]` selects the configuration. This is an explicit environment override—not a runtime failure fallback.
